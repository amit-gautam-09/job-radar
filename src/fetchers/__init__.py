"""Per-ATS fetchers. One module per ATS; each returns raw postings for normalize.py.

Field maps in each module were confirmed against LIVE responses on 2026-07-05
(Ground Rule 2). Implemented in Phase 2.
"""
